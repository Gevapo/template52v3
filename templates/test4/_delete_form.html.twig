<form method="post" action="{{ path('test4_delete', {'id': test4.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ test4.id) }}">
    <button class="btn">Delete</button>
</form>
