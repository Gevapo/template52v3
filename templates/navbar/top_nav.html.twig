<div class="navbar navbar-expand-lg fixed-top navbar-dark bg-primary">
    <div class="container">
        <a href="{{ path('homepage') }}" class="navbar-brand">Template 52v3</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarResponsive">

{#            <ul class="navbar-nav mr-auto">#}
{#                <li class="nav-item">#}
{#                    <a class="nav-link" href="#"><i class="fa fa-bread-slice" aria-hidden="true"></i>&nbsp;&nbsp;Home</a>#}
{#                </li>#}
{#            </ul>#}

{#            <ul class="navbar-nav mr-auto">#}
{#                <li class="nav-item">#}
{#                    <a class="nav-link text-danger" href=#">&nbsp;&nbsp;DEVELOPMENT</a>#}
{#                </li>#}
{#            </ul>#}

            <ul class="navbar-nav ml-auto">

                {% if is_granted('ROLE_VISITOR') %}
{#                {% if is_granted('ROLE_USER') %}#}

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" id="account">
                            <i class="fa fa-user" aria-hidden="true"></i><span class="caret"></span>&nbsp;
{#                            {% if app.user. %}&nbsp;{{ app.user.klant.voornaam }}{% endif %}#}
{#                            {% if app.user %}&nbsp;{{ app.user.klant.naam }}{% endif %}#}
                            {{ app.user.fullName }}

                        </a>
                        <div class="dropdown-menu" aria-labelledby="account">
                            <a class="dropdown-item" href="#">[Mijn gegevens]</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ path('logout') }}">Afmelden</a>
                        </div>
                    </li>

                {% else %}

                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('login') }}"><i class="fa fa-lock" aria-hidden="true"></i>&nbsp;&nbsp;  Aanmelden</a>
                    </li>
{#                    <li class="nav-item">#}
{#                        <a class="nav-link btn btn-outline-primary active" role="button" href="{{ path('register') }}" style="border-color: #FFF;">Registreer</a>#}
{#                    </li>#}

                {% endif %}

                {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" data-toggle="dropdown" href="#" id="admin"><i class="fa fa-tools" aria-hidden="true"></i><span class="caret"></span>&nbsp;&nbsp;Admin</a>
                    <div class="dropdown-menu" aria-labelledby="admin">
                        <a class="dropdown-item" href="{{ path('user_index') }}">Gebruikers</a>
{#                        <a class="dropdown-item" href="{{ path('contactpage') }}">contactpage</a>#}
{#                        <a class="dropdown-item"#}
{#                                {% if path('test_contact') %}#}
{#                                    href="{{ path('test_contact') }}"#}
{#                                {% else %}#}
{#                                    href="{{ path(app.request.attributes.get('_route'), app.request.attributes.get('_route_params')) }}"#}
{#                                {% endif %}#}

{#                           >test_contact</a>#}
{#                        <div class="dropdown-divider"></div>#}
{#                        <a class="dropdown-item" href="#">#</a>#}
                    </div>
                </li>
                {% endif %}

            </ul>

        </div>
    </div>
</div>

