{% extends 'base.html.twig' %}

{% block title %} | ADMIN wijzig {{ user.fullName }}{% endblock %}

{% block body %}

    {% include 'flash/flash.html.twig' %}

    <div class="container">
        <div class="bs-docs-section clearfix">

            <div class="row">
                <div class=" col-12">

                    <div class="gvp-vert-sp-3"></div>
                    <h3><b>Wijzig {{ user.fullName }}</b></h3>
                    <div class="gvp-vert-sp-3"></div>

                    {{ include('user/_form_edit.html.twig', {'button_label': 'Wijzigen'}) }}

                    {# <a href="{{ path('user_index') }}">back to list</a> #}

{#                    {{ include('user/_delete_form.html.twig') }}#}

                </div>
            </div>

        </div>
    </div>

{% endblock %}
